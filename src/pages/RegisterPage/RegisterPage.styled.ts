import styled from 'styled-components';

export const HeaderWrapper = styled.div`
    display: flex;
    justify-content: center;
    margin: 0 auto;
    width: fit-content;
    padding-top: 20px;
    overflow-y: hidden;
`